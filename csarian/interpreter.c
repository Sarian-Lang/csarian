// interpreter.c
#include <stdio.h>
#include <stdlib.h>

#include "definitions.h"

int interpreter(Token *tokens, int numTokens)
{
    for (size_t i = 0; i < numTokens; i++)
    {
        //...
    }
}